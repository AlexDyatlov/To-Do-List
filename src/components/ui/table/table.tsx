import TableHeader from './tableHeader';
import TableBody from './tableBody';

const Table = () => {
  return (
    <div>
      <TableHeader />
      <TableBody />
    </div>
  );
};

export default Table;
